import * as React from "react";

export default class Playground extends React.Component {
  public render() {
    return (
      <div>
        <p>如果你看到了这个，那说明你应该将这个文件拷贝一份到 Playground.tsx，然后可以开始你的表演。</p>
        <p>记住：Playground.default.tsx 不要修改或者将修改提交到代码库中，请修改拷贝之后的文件</p>
      </div>
    )
  }
}
